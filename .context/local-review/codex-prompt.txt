You are an adversarial code reviewer. Review the diff between main and HEAD on this repository (cwd is the repo root).

Use these commands to inspect:
- git log --oneline -20
- git show HEAD:README.md (and previous version: git show $(git merge-base HEAD main):README.md)
- git show HEAD:docs/index.html
- git show HEAD:docs/style.css
- git show HEAD:assets/logo-cardene.svg
- git show HEAD:.github/workflows/waka-readme.yml
- git show HEAD:.github/workflows/profile-3d.yml
- git show HEAD:.github/workflows/snake.yml (and previous: git show $(git merge-base HEAD main):.github/workflows/snake.yml)
- git show HEAD:profile-3d-contrib/settings.json

EXCLUDE FROM REVIEW: profile-summary-card-output/**/*.svg and profile-3d-contrib/profile-*.svg (auto-generated by GH Action, only timestamp updates).

CONTEXT: This PR redesigns a GitHub profile README + adds a GitHub Pages full-profile site (docs/index.html + docs/style.css) + CI workflows (waka-readme, profile-3d, snake) + an SVG logo + 3D contrib settings. Repo is github.com/cardene777/cardene777 — a personal profile repo, not production code.

FOCUS:
1. README.md: GitHub markdown sanitization compliance (no <style>/<script>/on*/background-color), external image URL availability/rate-limit, WakaTime <!--START_SECTION:waka--> marker pairing
2. docs/index.html: inline <style> + external style.css collision, image src reachability, leftover "[sample] cardene777 の 3D 草に置換予定" placeholder text shipped to main, Jekyll .nojekyll requirement for /docs publishing, raw HTML rendering issues
3. docs/style.css: scope leakage, OS font fallback for JetBrains Mono
4. .github/workflows/*.yml:
   - waka-readme.yml uses anmol098/waka-readme-stats@master (floating ref, supply chain)
   - profile-3d.yml: git push uses default actor, permissions block validity
   - snake.yml: outputs URL query parameter parsing safety
5. assets/logo-cardene.svg: <style><![CDATA[]]></style> rendering on GitHub raw SVG, glow filter sanitization, viewBox/cursor positioning
6. profile-3d-contrib/settings.json: schema validity, fileName matches README img path "profile-night-green.svg"

REJECT (treat as bugs even if "minor"):
- Floating action ref (master/main) instead of @vN or commit SHA
- External image URL with Mixed Content / 404 risk
- Leftover placeholder text ([sample], TODO, FIXME) shipped to main
- Workflow secrets printed to log
- Hardcoded user-specific paths
- README.md inline style usage that GitHub will sanitize away

SEVERITY:
- CRITICAL: secret leak, supply chain catastrophic, broken page rendering
- MAJOR: floating ref, leftover placeholder, missing permissions
- MINOR: style nits, deprecated HTML attributes, comment typos

OUTPUT FORMAT (max 30 findings):

## CRITICAL
- [path:line] summary — suggestion

## MAJOR
- [path:line] summary — suggestion

## MINOR
- [path:line] summary — suggestion

If no findings in a section, write "(none)".
